'use strict';jest.mock('../../EventEmitter/NativeEventEmitter');jest.setMock('../../BatchedBridge/NativeModules',{WebSocketModule:{connect:function connect(){}},PlatformConstants:{}});var WebSocket=require('../WebSocket');describe('WebSocket',function(){it('should have connection lifecycle constants defined on the class',function(){expect(WebSocket.CONNECTING).toEqual(0);});it('should have connection lifecycle constants defined on the instance',function(){expect(new WebSocket('wss://echo.websocket.org').CONNECTING).toEqual(0);});});