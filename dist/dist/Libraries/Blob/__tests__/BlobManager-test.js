'use strict';jest.setMock('../../BatchedBridge/NativeModules',{BlobModule:require('../__mocks__/BlobModule')});var Blob=require('../Blob');var BlobManager=require('../BlobManager');describe('BlobManager',function(){it('should create blob from parts',function(){var blob=BlobManager.createFromParts([],{type:'text/html'});expect(blob).toBeInstanceOf(Blob);expect(blob.type).toBe('text/html');});});